import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'dev.tuannguyen.vkufieldsurvey',
  appName: 'VKU Field Survey',
  webDir: 'www',
  server: { androidScheme: 'https' }
};

export default config;
