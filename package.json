{
  "name": "vku-field-survey",
  "version": "1.0.0",
  "type": "module",
  "private": true,
  "description": "PWA khảo sát cơ sở vật chất VKU, đóng gói Android bằng Capacitor.",
  "scripts": {
    "android:add": "cap add android",
    "android:sync": "cap sync android",
    "android:open": "cap open android",
    "android:build": "cd android && gradlew.bat assembleDebug"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.1",
    "@capacitor/cli": "^8.5.1",
    "@capacitor/core": "^8.5.1"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}
